
document.addEventListener("DOMContentLoaded",()=>{
    // 콜백 함수(빈 함수). 이벤트를 감지 그후 함수 실행
        document.getElementById("divmsg2").innerHTML="안녕";
    
    
    
    
    });
            function hello(n) {
            alert("Hello!버튼" + n);
       
       //---변수는 스크립트 내에----//
       const hi = 7; 
       const hin = () =>{}
       //const 값 바꿀 수 없음
       //let 값 바꿀 수 있음
       
       const hello = (n) => { //화살표 함수
        document.querySelector(".clmsg").innerHTML
         = "<h2>Hello!버튼" + n+"</h2>";
    }
                